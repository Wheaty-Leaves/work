#ifndef REVERSE_H
#define REVERSE_H

#include <string>

class Reverse
{
private:
public:
    Reverse();
    ~Reverse();
    int reverseDigit(int);
    std::string reverseString(std::string);
};

#endif